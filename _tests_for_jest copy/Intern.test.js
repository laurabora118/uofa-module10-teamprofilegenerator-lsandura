
// //const ita note means intern test a and so forth like an e value
//intern itz inter test z


// //intern object itz 
// //test the entire object of intern options
// test("can test intern object"), () => {
//     const itz = new Interninfo();
//     expect(typeof(itz)).toBe("object");
// };

///
//intern name
//test the entire test of the name option
test("test intern name"), () => {
    const testInput = "InternName";
    const ita = new Inamedata (iname);
    expect(ita.iname).toBe(testInput);
};

//id
//test the entire test of the id option
test("test intern id"), () => {
    const testInput = 100;
    const itb = new Iiddata ("Foo", testInput);
    expect(itb.mid).toBe(testInput);
};


//email
//test the entire test of the email option
test("test intern email"), () => {
    const testInput = 1;
    const itc = new Iemaildata ("Foo", 1, testInput);
    expect(itc.iemail).toBe(testInput);
};


//number
//test the entire test of the number option
test("test intern school"), () => {
    const testInput = "InternName";
    const ita = new Inamedata (iname);
    expect(ita.iname).toBe(testInput);
};